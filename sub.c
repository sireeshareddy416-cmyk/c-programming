#include <stdio.h>
int main()
{
    int a,b,sub;
    printf("\n enter the values of a and b is:");
    scanf("%d,%d",&a,&b);
    sub=a-b;
    printf("the value of a is %d and the value of b is %d and sub is %d\n",a,b,sub);
    return (0);
}
